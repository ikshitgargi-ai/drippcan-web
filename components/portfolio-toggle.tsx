'use client';

/**
 * Portfolio toggle — superseded for Dripp Tracker.
 *
 * The old fork toggled between two SKU books behind a client passcode.
 * Dripp Tracker has one book (Phoenix & Dayaa), so there is nothing to
 * toggle. Kept as a null-rendering stub so any straggler import compiles.
 * The limited external view is OWNER mode, enforced server-side.
 */
export function PortfolioToggle() {
  return null;
}
